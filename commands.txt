QXcbConnection: Could not connect to display
Aborted (core dumped)”

Install xming to run GUI from Windows WSL Linux
https://sourceforge.net/projects/xming/files/latest/download


git lfs install
git lfs track '*.model'
git lfs track '*.h5'

git log origin/master..HEAD
git diff --cached --name-only
git show --stat --oneline HEAD
git reset --soft HEAD^

conda create --name promotech_env python=3.6
conda env export > promotech_env.yml
conda info --envs
conda activate promotech_env
wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh
wget https://repo.anaconda.com/archive/Anaconda3-2020.07-Linux-x86_64.sh
pip freeze > requirements.txt

conda install numpy pandas matplotlib scipy scikit-learn progressbar2 XlsxWriter QtPy tensorflow==2.1.0
conda install -c bioconda/label/cf201901 java-jdk
conda install -c bioconda bedtools
conda install --channel conda-forge --channel bioconda pybedtools
conda install -c anaconda pyqt
conda install -c conda-forge scikit-bio
conda install -c conda-forge biopython
pip install 